import 'package:flutter/material.dart';

const OopsWidget oopsWidget = OopsWidget();

class OopsWidget extends StatelessWidget {
  const OopsWidget({super.key});

  @override
  Widget build(BuildContext context) => const Center(child: Text('Oops!'));
}
